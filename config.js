<script>
  // Para destravar a busca agora:
  // Quando quiser usar o catálogo do Supabase, troque para 'supabase'
  window.CATALOG_MODE = 'json';

  // Identity URL (robusto para webview)
  window.IDENTITY_URL = `${location.origin}/.netlify/identity`;

  // Admin (só cosmético – segurança real está nas functions)
  window.ADMIN_EMAIL = 'lucaslopessd@gmail.com';
</script>
